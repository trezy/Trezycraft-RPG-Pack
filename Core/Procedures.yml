EnableModule:
  type: procedure
  definitions: module
  script:
  - define success false

  - flag global enabledModules:->:%module%

  - inject s@<%module%> instantly

  - if <global.flag[enabledModules].as_list.find[%module%]> > 0 {
    - define success true
    }

  - determine <%success%>